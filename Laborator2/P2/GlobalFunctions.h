#pragma once
#include "Student.h"
namespace GlobalFunctions {
	namespace SimpleCompare {
		
		int byMath(Student& a, Student& b);
		int byHistory(Student& a, Student& b);
		int byEnglish(Student& a, Student& b);
		int byAvg(Student& a, Student& b);
		int byName(Student& a, Student& b);

	}
}
